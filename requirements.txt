# --- CPU-only torch index (avoids 2GB CUDA wheel crashing Render) ---
--extra-index-url https://download.pytorch.org/whl/cpu

# Core Backend Dependencies
fastapi==0.111.1
uvicorn[standard]==0.30.6
python-multipart==0.0.9
python-dotenv==1.0.1

# LangChain - stable 0.2.x pinned (Python 3.11, no Pydantic V1 crash)
langchain==0.2.16
langchain-community==0.2.16
langchain-core==0.2.40
langchain-text-splitters==0.2.4
langchain-groq==0.1.9

# Embedding Providers - CPU-only torch
sentence-transformers==2.7.0
torch==2.2.2+cpu

# Vector Store
faiss-cpu==1.8.0
numpy==1.26.4

# Document Processing
pypdf==4.3.1

# Data Processing
pandas==2.2.2

# Optional: Redis for caching
redis==5.0.8
